# =============================================================================
# Core dependencies
# =============================================================================
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0
sqlalchemy==2.0.25
aiosqlite==0.19.0

# =============================================================================
# Hardware (Raspberry Pi only — these are commented out so the project
# installs cleanly on dev/CI machines. The install.sh deployment script
# installs them separately on the target Pi.)
# =============================================================================
# RPi.GPIO==0.7.1
# spidev==3.6
# mfrc522==0.0.7
# pyserial==3.5

# =============================================================================
# Development & testing
# =============================================================================
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
black==23.12.1
ruff==0.1.13
mypy==1.8.0
