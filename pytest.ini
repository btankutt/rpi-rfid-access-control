[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts =
    --strict-markers
    --tb=short
    -ra

markers =
    hardware: tests that require physical hardware (skipped in CI)
    slow: tests that take > 1 second
